#!/bin/bash

findsize=$(ls -lah |cut -d ' ' -f 5)

if [[ ${size} -ge ${size1mls -la > my_files.txt} ]]
then echo "High" readlink -f ${directory} >> ~/ListOfBigDirs.txt

elif  [[ ${size} -ge ${size128k} ]]
then echo "Medium"

elif [[ ${size} -le ${size128k} ]]
then echo "Small"

fi